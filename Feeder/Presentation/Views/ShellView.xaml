<Window x:Class="Feeder.Presentation.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d" Title="{Binding Title}"
        Height="300" Width="300" MinHeight="700" MinWidth="470" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid Background="{StaticResource App.Background.Brush}">
        <!--<ContentControl x:Name="ActiveItem" />-->
        <Frame cal:Message.Attach="RegisterFrame($source)" DataContext="{x:Null}" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
